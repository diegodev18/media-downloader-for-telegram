services:
  app:
    build:
      context: .
      target: production
    container_name: media_downloader_for_telegram_app
    environment:
      - BOT_TOKEN
      - COOKIES
      - ALLOWED_CHAT_IDS
    restart: unless-stopped
