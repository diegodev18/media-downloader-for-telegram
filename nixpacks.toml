# Media Downloader for Telegram - Nixpacks
# Node + TypeScript build, runtime: yt-dlp + ffmpeg

[phases.setup]
# python3: requerido por el preinstall de yt-dlp-exec (solo en build)
nixPkgs = ["...", "ffmpeg", "yt-dlp", "python3"]

[start]
cmd = "pnpm start"
